# 新規開発を行うプロジェクト（つゆみ (Tsuyu-mi)）のディレクトリへターミナル上で移動します。

# 次回以降、該当プロジェクトの作業を再開する際は、システムが付与した連番でYOLO起動します。

DOS
   docker sandbox run gemini-1 -- --yolo

# （代替案・メンテナンス）不要になった過去の検証用コンテナ（gemini-1 等）がリソースを圧迫している場合は、速やかに以下のコマンドで完全破棄（パージ）し、ホスト環境をクリーンに保ってください。

DOS
docker sandbox rm gemini-1